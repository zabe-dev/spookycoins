# Optional. The app uses a public endpoint when these are omitted.
MARKET_DATA_API_URL=
MARKET_DATA_API_KEY=

# Required once persistent application data is enabled.
DATABASE_URL=

# Optional and used only when regenerating the canonical dataset.
DATASET_POPULATED_AT=
